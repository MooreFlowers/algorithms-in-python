[project]
name = "algorithms-in-python"
version = "0.1.0"
description = "A curated collection of Python algorithms and data structures."
requires-python = ">=3.12"

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
pythonpath = ["."]